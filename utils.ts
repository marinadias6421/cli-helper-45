type CryptoCurrency = {\n    id: string;\n    name: string;\n    symbol: string;\n    currentPrice: number;\n    marketCap: number;\n    volume24h: number;\n    circulatingSupply: number;\n};\n\ntype CryptoResponse = {\n    data: CryptoCurrency[];\n    status: {\n        timestamp: string;\n        currency: string;\n    };\n};\n\nconst fetchCryptoData = async (url: string): Promise<CryptoResponse> => {\n    const response = await fetch(url);\n    if (!response.ok) {\n        throw new Error('Network response was not ok');\n    }\n    return response.json();\n};\n\nconst filterHighMarketCap = (cryptos: CryptoCurrency[], threshold: number): CryptoCurrency[] => {\n    return cryptos.filter(crypto => crypto.marketCap > threshold);\n};\n\nexport { fetchCryptoData, filterHighMarketCap, CryptoCurrency, CryptoResponse };